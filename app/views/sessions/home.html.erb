<style>
@import url('https://fonts.googleapis.com/css?family=Staatliches&display=swap');
  * {
    font-family: 'Staatliches', cursive;
  }
</style>

<header>

</header>

<img id="beer-club-logo" src="/BeerClubLogo.png" alt="Beer Club Logo">
<div class="session-container">
<h1 class="session-title">Welcome to Beer Club</h1>
<h2 class="session-title">Newly Reviewed Beers</h2>

<ul id="newly-reviewed-beers-list">
<% Beer.new_to_beer_club.each do |beer| %>
<li><%=beer.reviews.first.date.strftime('%B %e, %Y') %> -  <%= beer.name %></li>
<% end %>
</ul>

<br>

<% if @user && @user.errors.any? %>
  <div id="error_explanation">
    <% @user.errors.messages.each do |msg| %>
      <%= msg[1].first %>
    <% end %>
  </div>
  <br>
<% end %>

<p><%= form_tag ({controller: 'sessions', action: 'login', method: 'post'}) do %>
   <%= text_field_tag :username, "", placeholder: 'Username' %>
   <%= password_field_tag :password, "", placeholder: "Password" %>
   <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
   <%= submit_tag "Login" %>
<% end %></p>
<p><%= button_tag(:type => 'button', :class => 'button') do %>
<%= link_to('Log in with GitHub', '/auth/github') %>
<% end %></p>
<p><%= button_tag(:type => 'button', :class => 'button') do %>
  <% link_to "Create an Account", new_user_path, { :class=>"button_link" } %>
<% end %></p>
</div>

